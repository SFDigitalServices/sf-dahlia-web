accordion-heading
  img.accordion-icon src="/images/listing-units.svg" alt="neighborhood-building"
  h3.accordion-name
    | Lottery
  span.accordion-subheader Lottery selection, important dates and contact
  span.ui-control.ui-icon.ui-small
    svg
      use xlink:href="#i-arrow-down"

section.content-wrap
  .content-group.no-padding.hide-for-small-down
    span.post-dealine_alert
      .label.flag-label.secondary.text-wrap.alert-tint
        span.ui-icon.ui-static.ui-medium.i-color
          svg
            use xlink:href="#i-clock"
        | Applications Closed {{listing.Application_Due_Date | date : "MMM d, yyyy 'at' h:mm a"}}

  span ng-if="lotteryDateVenueAvailable(listing) && !listingHasLotteryResults()"
    ng-include src="'listings/templates/listing/_pre_lottery_info.html'"

  .content-group ng-if='!listing.Lottery_Results && !listingIsFirstComeFirstServe()'
    h4.h-caps-lined Lottery Results
    p.content-group_date
      span.content-group_day
        | {{ listing.Lottery_Results_Date | date : 'longDate' }}
    p.c-steel.t-small Complete lottery results will be posted on this date by 5pm.

  .content-group.expand.text-center ng-if="listingHasLotteryResults()"
    ng-include src="'listings/templates/listing/_lottery_results.html'"

  .content-group
    ng-include src="'listings/templates/listing/_what_to_expect.html'"

  / neighborhood preference list
  .content-group ng-if="showPreferenceListPDF()"
    h4.content-group_title ng-if="listingHasPreference('neighborhoodResidence')"
      | {{'LISTINGS.CONFIRMED_NRHP_LIST' | translate}}
    h4.content-group_title ng-if="listingHasPreference('antiDisplacement')"
      | {{'LISTINGS.CONFIRMED_ADHP_LIST' | translate}}
    a.button.primary.expand ng-href="{{listing.NeighborHoodPreferenceUrl}}" target="_blank"
      | View Preference List
    p.c-steel.t-small
      | Applications that qualify for this preference will be given a higher priority in the lottery.

  .content-group ng-if="leasingAgentInfoAvailable()"
    ng-include src="'listings/templates/listing/_contact_leasing_agent.html'"

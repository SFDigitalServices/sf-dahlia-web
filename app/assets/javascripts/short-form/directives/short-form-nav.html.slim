dl.application-form_nav.sub-nav ng-show="show()"
  dd ng-repeat="section in sections" ng-class="{active: isActiveSection(section)}"
    button ui-sref="dahlia.short-form-application.{{ section.pages[0] }}" ng-disabled="!(isActiveSection(section) || isPreviousSection(section))"
      | {{section.name}}

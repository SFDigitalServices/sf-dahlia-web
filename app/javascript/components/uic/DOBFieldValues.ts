/* Vendored from @bloom-housing/ui-components src/forms/DOBField.tsx.
   Only the DOBFieldValues type is used by this repo; the DOBField component
   itself (dayjs + react-hook-form) was not vendored. */
export type DOBFieldValues = {
  birthDay: string
  birthMonth: string
  birthYear: string
}

{
  "compilerOptions": {
    "baseUrl": "http://localhost:3000/",
    "noEmit": true,
    // be explicit about types included
    // to avoid clashing with Jest types
    "types": ["cypress"]
  },
  "exclude": ["app/javascript/**/*.test.tsx"],
  "include": ["**/*.ts"]
}
